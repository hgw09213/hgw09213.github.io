// hw11
public class Main {
	public static void main(String[] args) {
		int data[] = new int[100];
		int length = data.length, i, j, t;
		double a = Math.ceil(length/2), b = Math.floor(length/2);
		double arithmetic = 0, geometric = 1, harmonic, median;
		for (i = 0; i < length; i++) {
			data[i] = (int) (Math.random() * 100);
		}
		for (i = 0; i < length; i++) {
			for (j = 0; j < 99-i; j++) {
				if (data[j] > data[j + 1]) {
					t = data[j];
					data[j] = data[j + 1];
					data[j + 1] = t;
				}
			}
		}
		for (i = 0; i < length; i++) {
			arithmetic += data[i];
			geometric *= data[i];
		}
		
		arithmetic /= length;
		geometric = Math.pow(geometric, 1./length);
		harmonic = Math.pow(geometric, 2) / arithmetic;
		median = (data[(int)a] + data[(int)b]) / 2;
		
		
		System.out.printf("arithmetic: %.2f\ngeometric: %.2f\nharmonic: %.2f\nmedian: %.2f", arithmetic, geometric, harmonic, median);
	}
}

// 중간 문제, AND OR 답 판별
public class Main {
  public static void main(String[] args) {
    boolean isLoggedIn = true;
    boolean isAdmin = false;
    int securityLevel = 3; // 1 = highest

    if (isLoggedIn && (isAdmin || securityLevel <= 2)) {
      System.out.println("Access granted");
    } else {
      System.out.println("Access denied");
    }

  // Try changing securityLevel to test different outcomes:
  // securityLevel 1 = Access granted
  // securityLevel 2 = Access granted
  // securityLevel 3 = Access denied
  // securityLevel 4 = Access denied
  // If isAdmin = true, access is granted.
  }
}

// 중간 문제 Java Method Overloading
public class Main {
  static int plusMethod(int x, int y) { // 매개변수가 int 2개
    return x + y;
  }  
  
  static int plusMethod(int x) { // 매개변수가 int 1개
    return x + 10;
  }
  
  static double plusMethod(double x, double y) { // 매개변수가 double 2개
    return x + y;
  }
  
  public static void main(String[] args) {
    int myNum1 = plusMethod(8);
    double myNum2 = plusMethod(4.3, 6.26);
    System.out.println("int: " + myNum1);
    System.out.println("double: " + myNum2);
  }
}

몇 번 함수가 호출 되었는가? <-- 함수의 이름이 같아도 매개변수가 다르다면 사용가능

  static int plusMethod(int x, int y) {
    return x + y;
  }  

  static int plusMethod(int y, int x) {
    return x + y;
  }  

하지만 위 문장처럼 타입과 인자의 수가 같지만 서로 순서만 바뀐 경우는 불가

