파일 입출력  
FileReader, FileWriter : 텍스트 파일을 읽는/쓰는 클래스
FileInputStream, FileOutputStream : 바이트 단위로 바이트 데이터를 읽는/쓰는 클래스
DataInputStream, DataOutputStream : 프리미티브 타입(int, float, double) 데이터를 읽는/출력하는 클래스

// 시험 문제 Iterator 두 가지 방법 for, while
import java.util.ArrayList;
import java.util.Iterator;

public class Main {
  public static void main(String[] args) {
  
    // Make a collection
    ArrayList<String> cars = new ArrayList<String>();
    cars.add("Volvo");
    cars.add("BMW");
    cars.add("Ford");
    cars.add("Mazda");
  
    // Get the iterator
    Iterator<String> it = cars.iterator();
    while (it.hasNext()) { // while 이용 
      String str = it.next();
      System.out.println(str);
    }
    
    System.out.println("enhanced for loop");
    for (String str : cars) { // for 이용
    System.out.println(str);
	}
  
    System.out.println("standard for loop");
    for(int i = 0; i < cars.size(); i++) { // 원시적인 방법
    	System.out.println(cars.get(i));
    }
  }
}

